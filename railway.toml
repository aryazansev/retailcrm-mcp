# Railway Simple Config
[build]
  builder = "dockerfile"

[deploy]
  startCommand = "node build/test-server.js"
  healthcheckPath = "/health"
  restartPolicyType = "ON_FAILURE"

[env]
  NODE_ENV = "production"