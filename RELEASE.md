# Руководство по выпуску релиза

## 📦 Подготовка к публикации

### 1. Создайте репозиторий на GitHub

```bash
# Создайте новый репозиторий на GitHub
git init
git add .
git commit -m "Initial release"
git branch -M main
git remote add origin https://github.com/yourusername/retailcrm-mcp.git
git push -u origin main
```

### 2. Обновите package.json

Замените в `package.json`:
- `yourusername` на ваш GitHub username
- `author` на ваше имя

### 3. Создайте GitHub Release

```bash
# Создайте тег
git tag -a v1.0.0 -m "Release version 1.0.0"
git push origin v1.0.0
```

Затем на GitHub:
1. Перейдите в раздел **Releases**
2. Нажмите **"Create a new release"**
3. Выберите тег v1.0.0
4. Добавьте описание релиза
5. Опубликуйте

## 🚀 Публикация в npm

### 1. Создайте аккаунт npm

Если ещё нет:
```bash
npm adduser
```

### 2. Войдите в npm

```bash
npm login
```

### 3. Опубликуйте пакет

```bash
npm publish --access public
```

**Важно:** Перед публикацией убедитесь что:
- Версия в `package.json` правильная
- Нет `.env` файла в `files` (уже настроено)
- Проект собран (`npm run build`)

## 📋 Чеклист перед релизом

- [ ] Код очищен от debug-логов
- [ ] Проект собирается без ошибок (`npm run build`)
- [ ] Все зависимости в `package.json`
- [ ] README.md полный и актуальный
- [ ] LICENSE файл создан
- [ ] .env.example создан
- [ ] install.sh исполняемый (`chmod +x install.sh`)
- [ ] Версия в package.json обновлена
- [ ] GitHub репозиторий создан
- [ ] Тег создан и запушен

## 🔄 Обновление версии

При обновлении:

1. Измените версию в `package.json` (например, "1.0.1")
2. Сделайте commit: `git commit -am "Bump version to 1.0.1"`
3. Создайте тег: `git tag -a v1.0.1 -m "Version 1.0.1"`
4. Запушьте: `git push && git push origin v1.0.1`
5. Опубликуйте: `npm publish`

## 📥 Установка для пользователей

После публикации пользователи могут установить:

```bash
# Через npm
npm install -g retailcrm-mcp

# Или через install.sh
curl -fsSL https://raw.githubusercontent.com/yourusername/retailcrm-mcp/main/install.sh | bash
```
