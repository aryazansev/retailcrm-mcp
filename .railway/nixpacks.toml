# Simple Nixpacks configuration for Railway
[phases.build]
  cmds = ["npm run build"]

[start]
  cmd = "node build/server.js"

# Health check configuration
[healthcheck]
  path = "/health"
  interval = 10
  timeout = 5
  retries = 3

[variables]
  NODE_ENV = "production"
  MCP_PORT = "3002"